path = ~/.lexaloffle/pico-8/carts/

install:
	cp "game.p8" ${path}
	cp "doors.lua" ${path}
	cp "handlers.lua" ${path}
	cp "items.lua" ${path}
	cp "main.lua" ${path}
	cp "plates.lua" ${path}
	cp "utility.lua" ${path}